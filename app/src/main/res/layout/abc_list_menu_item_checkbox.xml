<?xml version="1.0" encoding="utf-8"?>
<!-- app:buttonTint paints the box, app:buttonIconTint the tick inside it. The tick takes the menu's own
     surface colour, which is by definition what colorTopBarIcon is chosen to be legible against, so the
     pair stays readable in every theme. Do not add android:buttonTint here: MaterialCheckBox stacks the
     box and the tick into one drawable, and the platform tint covers that whole stack, flattening the
     tick into the filled box. -->
<CheckBox xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:id="@+id/checkbox"
	android:layout_width="wrap_content"
	android:layout_height="wrap_content"
	android:layout_gravity="center_vertical"
	android:clickable="false"
	android:duplicateParentState="true"
	android:focusable="false"
	android:minWidth="0dp"
	android:minHeight="0dp"
	android:paddingStart="0dp"
	android:paddingEnd="0dp"
	android:paddingLeft="0dp"
	android:paddingRight="0dp"
	app:buttonIconTint="?attr/colorSurfaceContainer"
	app:buttonTint="?attr/colorTopBarIcon" />
