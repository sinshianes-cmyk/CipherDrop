<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	tools:context=".settings.sources.catalog.SourcesCatalogActivity">

	<com.google.android.material.appbar.AppBarLayout
		android:id="@+id/appbar"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:background="?attr/colorSurface"
		android:elevation="0dp"
		android:fitsSystemWindows="false"
		android:stateListAnimator="@null"
		app:elevation="0dp"
		app:liftOnScroll="false"
		app:liftOnScrollColor="@null">

		<com.google.android.material.appbar.CollapsingToolbarLayout
			android:id="@+id/collapsingToolbarLayout"
			style="?attr/collapsingToolbarLayoutMediumStyle"
			android:layout_width="match_parent"
			android:layout_height="?attr/collapsingToolbarLayoutMediumSize"
			app:layout_scrollFlags="scroll|exitUntilCollapsed|snap"
			app:toolbarId="@id/toolbar">

			<!-- Stand-in for the expanded title while search is open: Toolbar hides the CTL's
			     title anchor during search, so the CTL can't draw the title even when expanded.
			     Styled and positioned in SourcesCatalogActivity via bindExpandedSearchTitle. -->
			<TextView
				android:id="@+id/text_title_search"
				android:layout_width="wrap_content"
				android:layout_height="wrap_content"
				android:layout_gravity="bottom|start"
				android:singleLine="true"
				android:ellipsize="end"
				android:includeFontPadding="false"
				android:visibility="gone"
				app:layout_collapseMode="parallax" />

			<com.google.android.material.appbar.MaterialToolbar
				android:id="@id/toolbar"
				android:layout_width="match_parent"
				android:layout_height="?attr/actionBarSize"
				android:background="@null"
				app:layout_collapseMode="pin" />

		</com.google.android.material.appbar.CollapsingToolbarLayout>

		<org.koitharu.kotatsu.core.ui.widgets.NestedHorizontalScrollView
			android:id="@+id/scrollView_chips"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:clipToPadding="false"
			android:paddingHorizontal="@dimen/list_spacing_large"
			android:scrollbars="none">

			<org.koitharu.kotatsu.core.ui.widgets.ChipsView
				android:id="@+id/chips_filter"
				android:layout_width="wrap_content"
				android:layout_height="wrap_content"
				android:clipChildren="false"
				android:clipToPadding="false"
				android:paddingVertical="@dimen/margin_small"
				app:chipStyle="@style/Widget.Kotatsu.Chip.Filter"
				app:selectionRequired="false"
				app:singleLine="true"
				app:singleSelection="false" />

		</org.koitharu.kotatsu.core.ui.widgets.NestedHorizontalScrollView>

		<com.google.android.material.tabs.TabLayout
			android:id="@+id/tabs"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:background="@null"
			app:tabGravity="start"
			app:tabMode="scrollable" />

	</com.google.android.material.appbar.AppBarLayout>

	<androidx.viewpager2.widget.ViewPager2
		android:id="@+id/pager"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior" />

	<com.google.android.material.floatingactionbutton.FloatingActionButton
		android:id="@+id/button_scroll_to_top"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:layout_gravity="bottom|center_horizontal"
		android:layout_marginBottom="@dimen/margin_normal"
		android:contentDescription="@string/back_to_top"
		android:visibility="gone"
		app:backgroundTint="?attr/colorSurface"
		app:borderWidth="0dp"
		app:elevation="0dp"
		app:fabCustomSize="40dp"
		app:maxImageSize="18dp"
		app:pressedTranslationZ="0dp"
		app:srcCompat="@drawable/ic_arrow_up"
		app:tint="?attr/colorOnSurfaceVariant" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
