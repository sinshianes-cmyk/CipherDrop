<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:layout_width="match_parent"
	android:layout_height="match_parent">

	<com.google.android.material.appbar.AppBarLayout
		android:id="@+id/appbar"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:fitsSystemWindows="false"
		app:elevation="0dp"
		app:liftOnScroll="false">

		<!-- The filter badge is attached with this FrameLayout as its custom parent, so it is drawn
		     as the frame's foreground instead of inside the menu item's own ViewOverlay - the
		     overlay clips to the icon's bounds and cuts the badge off. -->
		<FrameLayout
			android:id="@+id/toolbarContainer"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			app:layout_scrollFlags="scroll|enterAlways">

			<com.google.android.material.appbar.MaterialToolbar
				android:id="@id/toolbar"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:minHeight="72dp"
				android:paddingTop="@dimen/margin_small"
				android:paddingBottom="@dimen/margin_small"
				app:contentInsetStartWithNavigation="@dimen/margin_normal" />

		</FrameLayout>

	</com.google.android.material.appbar.AppBarLayout>

	<org.koitharu.kotatsu.core.ui.list.fastscroll.FastScrollRecyclerView
		android:id="@id/recyclerView"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:orientation="vertical"
		app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
		app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior" />

	<!-- Status bar protection: the same surface-to-transparent fade as the main screen, sized from the top inset. -->
	<View
		android:id="@+id/statusBarScrim"
		android:layout_width="match_parent"
		android:layout_height="0dp"
		android:layout_gravity="top"
		android:importantForAccessibility="no" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
